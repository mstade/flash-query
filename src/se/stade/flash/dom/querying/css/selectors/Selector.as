package se.stade.flash.dom.querying.css.selectors
{
    import se.stade.flash.dom.querying.ElementMatcher;
    import se.stade.parsing.Expression;
    
    public interface Selector extends ElementMatcher, Expression
    {
    }
}
