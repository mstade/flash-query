package se.stade.flash.dom.query.css.parsing.tokens
{
	import se.stade.flash.dom.query.parsing.StringToken;
	
	public class HashToken extends StringToken
	{
		public function HashToken(index:int, match:String)
		{
			super(index, match);
		}
	}
}